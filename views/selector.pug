extends layout

block content
  h1= title
  p Welcome to #{title}. My name is RaaviAdithyakrishna.
  - var image_names = ["kajal.jpg","krithi.jpg","vidya2.jpg","pooja.jpg","aish1.jpg"]
  - for(let i=0; i<3; i++){
    - var j=Math.floor(Math.random()*5);
    - var img_j=image_names[j];
    image(height="300" width="300" src=`\\images\\${img_j}` alt=`${img_j}` )
  - }